name = input("Mi a neved?: ")
# int woks only with numbers, if the input is not a number, it will raise a ValueError
age = int(input("Hány éves vagy?: "))

print(name, type(name))
print(age, type(age))
